On this Branch:
- Collision algorithm is:
- - based on (a single) flag 
    (instead of relative-speed-based)
- - pair-wise processing, without storing new position and speed as temp
    (instead of processing one object at a time, storing new position and speed as temp)
